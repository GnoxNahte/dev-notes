formulas:
  Created day: '[html("<span style=\"display:none;\">" + file.ctime.format("YYYY-DD-MM")+ "</span>"), file.mtime.format("DD MMM YYYY")]'
  Created time: file.ctime.format("DD MMM YYYY, hh:mm A")
views:
  - type: table
    name: Table
    filters:
      and:
        - file.inFolder(this.file.folder) && file.path != this.file.path
    groupBy:
      property: formula.Created day
      direction: DESC
    order:
      - file.name
      - formula.Created time
    sort:
      - property: file.ctime
        direction: DESC
